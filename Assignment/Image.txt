/*
 Author:				Cameron-Murphy
 Class:					GPR-200-01
 Assignment:			Lab 6: Intro to Post Processing & Composition
 Due Date:              10/15/20
*/

// GLSL STARTER CODE BY DANIEL S. BUCKSTEIN
//  -> IMAGE TAB (final)

//------------------------------------------------------------
// SHADERTOY MAIN

// mainImage: process the current pixel (exactly one call per pixel)
//    fragColor: output final color for current pixel
//    fragCoord: input location of current pixel in image (in pixels)
void mainImage(out color4 fragColor, in sCoord fragCoord)
{
    // test UV for input image
    sCoord uv = fragCoord / iChannelResolution[0].xy;
    
    //Determines colors to merge
    color4 firstColor = texture(iChannel0, uv);
    color4 secondColor = texture(iChannel1, uv);
    
    //Base
    //fragColor = texture(iChannel1, uv);
    
    //Add
    fragColor = firstColor + secondColor;
    
    //Mix
    //float mixBrightness = 1.0;
    //fragColor = mix(firstColor, secondColor, mixBrightness);
    
    //Screen
    //fragColor = 1.0 - (1.0 - firstColor) * (1.0 - secondColor);
    
    //Color Dodge
    //fragColor = secondColor / (1.0 - firstColor);
}